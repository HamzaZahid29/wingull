import 'package:flutter/material.dart';

class AppPaddings {
  static EdgeInsets normalPadding1 = EdgeInsets.all(8.0);
  static EdgeInsets normalPadding2 = EdgeInsets.all(12.0);
  static EdgeInsets normalPadding3 = EdgeInsets.all(18.0);
}
